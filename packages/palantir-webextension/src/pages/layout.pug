include ../fragments/fragments.pug

doctype html
html
  head
    block head
      if target != "firefox"
        script(src=include("+webextension-polyfill.mjs"))
      script(src=include("+head.ts"))
      link(rel="icon" href=asset("images/favicon.ico"))
      link(rel="stylesheet" href=include("pages/+common.scss"))
  body
    block content
    block scripts
      script(src=include("+common.ts"))
